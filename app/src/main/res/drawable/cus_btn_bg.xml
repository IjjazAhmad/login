<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background shape -->
    <item>
        <shape
            android:shape="rectangle">
            <solid android:color="#0ACF83" /> <!-- Change color to your desired background color -->
            <corners android:radius="8dp" /> <!-- Adjust corner radius as needed -->
        </shape>
    </item>

    <!-- Shadow (if desired) -->
    <item android:right="1dp" android:left="1dp" android:top="1dp" android:bottom="1dp">
        <shape
            android:shape="rectangle">
            <solid android:color="#80000000" /> <!-- Change color to your desired shadow color -->
            <corners android:radius="8dp" /> <!-- Should match the corner radius above -->
        </shape>
    </item>

</layer-list>
